# Ensure packages are at least 1 week old before allowing installation
minimum-release-age=10080

# Auto-install peer dependencies
auto-install-peers=true

# Dedupe peer dependencies for cleaner installs
dedupe-peer-dependents=true
