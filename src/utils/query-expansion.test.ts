import { describe, it, expect } from "vitest"
import { expandQuery } from "./query-expansion"

describe("expandQuery", () => {
    it("should exist", () => {
        expect(expandQuery).toBeDefined()
    })
})